# ClassPets 班級系統 — 使用手冊

> 版本：v120（2026-07-09）  
> 適用對象：教師、班級管理員

---

## 一、系統簡介

ClassPets 是一套班級獎懲與神獸養成系統，結合：

- **學生分數管理**（加分、減分、復原）
- **3D 神獸模型**（達標孵化）
- **組別加分**
- **課堂任務、抽籤、計時器**等輔助工具
- **Firebase 雲端同步**（同一班級代碼的裝置可即時共用資料）

---

## 二、首次進入

### 1. 開啟網頁

建議使用 **http://127.0.0.1:8080** 或已部署的網址開啟。  
若直接以 `file://` 開啟，3D 模型可能無法載入，請改用專案內的 `serve.cmd` 啟動本地伺服器。

### 2. 網站密碼

首次進入會要求輸入網站密碼（預設由校方設定）。輸入正確後即可進入主畫面。

### 3. 連接班級雲端

進入後請輸入**班級代碼**（例如 `1b`、`3a`）：

- 每位教師／每班可使用不同代碼，資料互不干擾
- 連線成功後，分數、組別、任務、常用網頁等會同步至雲端
- 同一班級代碼可在電腦、平板、手機同時使用並即時更新

---

## 三、一般模式與編輯模式

### 一般模式（預設）

- 可為學生**加減分**
- 可點擊分數開啟快捷加減分選單
- 可查看神獸、成長日誌
- 可使用計時器、抽籤、常用網頁等**非編輯**功能

### 編輯模式

- 點擊左上角 **✏️ 編輯模式** 按鈕，**連擊兩下**即可開啟／關閉
- 功能欄底部會顯示提示：
  - 未開啟：**「進入編輯模式，開啟更多功能。」**（金色醒目提示）
  - 已開啟：**「已進入編輯模式。」**（綠色提示）

編輯模式額外功能包括：

| 功能 | 說明 |
|------|------|
| 修改學生姓名 | 點擊學生卡片或姓名 |
| 更改神獸動物 | 點擊 3D 模型 |
| 指定組別 | 透過學生卡片選單 |
| 新增／移除學生 | 班級設定 |
| 修改園地名稱 | 班級設定 |
| 編輯加分任務內容 | 加分任務面板 |
| 管理常用網頁 | 新增／刪除連結 |
| 切換班級、手動備份 | 功能欄底部 |
| 復原／重做分數 | Ctrl+Z / Ctrl+Y |

---

## 四、學生分數操作

### 快捷加減分（一般模式）

1. 點擊學生卡片下方的**分數數字**
2. 彈出快捷選單，可選擇 +1、+2、+5… 或對應扣分
3. 加分／扣分時會有音效與畫面提示

### 精確調整（編輯模式）

點擊分數後可輸入：

- `+20`、`-10`：在目前分數上加減
- `80`：直接設為 80 分

### 復原分數

編輯模式下可使用 **Ctrl+Z** 復原上一步分數操作，**Ctrl+Y** 重做。

### 睡眠狀態

當學生生命值歸零時進入睡眠，**無法加分**（需先恢復生命值）。

---

## 五、神獸與孵化

- 每位學生初始為**蛋**，分數達 **20 分**後自動孵化為 3D 神獸
- 點擊已孵化的神獸可進入**個人神獸頁面**（`pet.html`）
- 分數越高，卡片會出現越豐富的視覺特效（光暈、星星、皇冠等）
- 編輯模式下可點擊神獸更改動物種類

---

## 六、組別加分

- 畫面上有**可拖曳的組別面板**
- 點擊組別名稱可為整組加分
- 編輯模式下可管理組別成員與名稱

---

## 七、功能欄（🛠️ Tools）

點擊右側 **🛠️ 功能** 標籤開啟側邊欄。

### 功能 1：加分任務

- **抽取任務**：隨機抽出本節課任務，顯示於畫面任務面板
- **齊心協力**任務會額外顯示全班累積得分進度條（目標 50 分）
- 任務達成後可點「任務達成」給予全班獎勵加分

**編輯任務內容（需編輯模式）：**

1. 展開「加分任務」面板
2. 在下方編輯各任務的**標題、內容、獎勵說明**
3. 按 **儲存任務內容**（自動同步雲端）
4. 可按 **還原預設** 恢復原始五項任務

### 功能 2：幸運抽籤

- 設定抽籤人數與加分值
- 隨機抽出學生並加分

### 功能 3：課堂計時器

- **碼表**與**倒計時**兩種模式
- 可設定提示音、倒計時間隔提示
- 倒計時可按「開始」放大全螢幕顯示

### 常用網頁

- 每班最多 **10 條**連結
- 儲存後**自動同步雲端**，同班裝置共用
- 一般模式可點擊開啟；編輯模式可新增／刪除

### 班級設定（編輯模式）

- 修改**園地名稱**（主畫面標題）
- **新增／移除學生**（1～40 人）

### 切換班級（編輯模式）

- 輸入新班級代碼並切換
- 切換前會自動儲存目前班級資料至雲端

### 手動備份資料（編輯模式）

- **複製存檔碼**：匯出全班資料到一串文字
- **還原存檔碼**：貼上先前備份以覆蓋本機資料（並同步雲端）

---

## 八、雲端同步說明

| 資料類型 | 是否上雲端 |
|----------|------------|
| 學生分數、神獸、生命值 | ✅ |
| 組別 | ✅ |
| 加分任務內容與狀態 | ✅ |
| 常用網頁 | ✅ |
| 計時器設定 | ✅ |
| 班級進度、每日得分紀錄 | ✅ |
| 編輯模式開關 | ❌（本機） |

畫面上方或班級設定區會顯示雲端連線狀態。若同步失敗，請檢查網路與 Firebase 設定。

---

## 九、成長日誌

點擊學生卡片右上角的 **📜** 可查看該生近 **7 日**得分變化。

---

## 十、開發人模式（進階）

- 在編輯模式下，**連續點擊版本號徽章 4 次**（短時間內）可進入開發人模式
- 提供額外除錯與實驗功能（一般教師無需使用）

---

## 十一、常見問題

**Q：手機加分後，電腦沒有更新？**  
A：請確認兩邊皆已連接**相同班級代碼**，且網路正常。

**Q：3D 模型顯示不出來？**  
A：請勿用 `file://` 開啟，改用 `serve.cmd` 後以 `http://127.0.0.1:8080` 存取。

**Q：如何讓另一位老師使用獨立資料？**  
A：使用不同的班級代碼即可，雲端資料完全分開。

**Q：任務內容改完後其他裝置會更新嗎？**  
A：會。儲存任務內容後會同步至雲端，其他已連線裝置會自動載入。

**Q：忘記編輯模式怎麼開？**  
A：看功能欄底部的金色提示「進入編輯模式，開啟更多功能。」，然後到左上角連擊兩下 ✏️ 按鈕。

---

## 十二、快速操作流程（一節課建議）

1. 開啟網頁 → 輸入密碼 → 連接班級代碼  
2. （可選）抽取或編輯本節加分任務  
3. 上課期間為學生加減分  
4. （可選）使用計時器、抽籤、組別加分  
5. 下課前可手動備份存檔碼作額外保存  

---

*如有功能更新，請以畫面版本號（右下角或編輯模式下右上角）為準。*
